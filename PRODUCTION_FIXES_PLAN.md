# ПОЛКА — План устранения замечаний PRODUCTION_READINESS

> Источник замечаний: [PRODUCTION_READINESS.md](PRODUCTION_READINESS.md)
> Составлен: 12.06.2026

Пункты разделены по **исполнителю** и сгруппированы в фазы по приоритету.

## Условные обозначения
- 🤖 — код в репозитории (делает разработчик/агент)
- 🔧 — инфраструктура/сервер: скрипт + документация от разработчика, выполнение на проде — оператором
- 📄 — контент: нужны реальные тексты/реквизиты (юрист/владелец)

Объём: **S** — мелко, **M** — средне, **L** — крупно.

---

## Фаза 0 — Блокеры, код ✅ ВЫПОЛНЕНО

| # | Замечание | Что сделано | Статус |
|---|---|---|---|
| 1.1 | Вебхук ЮKassa: одиночные IP + спуфинг `X-Forwarded-For` | 🤖 `src/lib/ip.ts` (`ipInCidr`, `clientIp`), вебхук на CIDR + `X-Real-IP`/последний XFF; тот же источник IP в `src/proxy.ts` | ✅ |
| 1.4 | Seed с тест-аккаунтами может попасть на прод | 🤖 `prisma/seed.ts` безопасен; демо → `scripts/seed-demo.ts`; guard `scripts/guard.ts` (`NODE_ENV=production`) на всех демо-сидах; `scripts/create-admin.ts`; чеклист DEPLOY | ✅ |

## Фаза 0 — Важное, быстрые победы кодом ✅ ВЫПОЛНЕНО

| # | Замечание | Что сделано | Статус |
|---|---|---|---|
| 2.4 | Нет health-эндпоинта | 🤖 `src/app/api/health/route.ts` (`SELECT 1`) + healthcheck `app`/`db` в compose + `depends_on: service_healthy` | ✅ |
| 2.5 | Нет security-заголовков | 🤖 `headers()` в `next.config.ts` (HSTS, X-Frame-Options, nosniff, Referrer-Policy, Permissions-Policy) + nginx-блок в DEPLOY.md | ✅ |
| 2.3 | Postgres торчит наружу, пароль `password` | 🤖 база — порт на `127.0.0.1`; `docker-compose.prod.yml` (`ports: !reset []`, пароль из env) + доку | ✅ |
| 3.1 | Нестыковка ключа rate-limit (`payment` userId vs IP) | 🤖 убрана IP-копия из `proxy.ts` (лимит по userId в роуте), источник IP → `clientIp` | ✅ |

## Фаза 0 — Восстановление пароля ✅ ВЫПОЛНЕНО

| # | Замечание | Что сделано | Статус |
|---|---|---|---|
| 2.2 | Нет восстановления пароля | 🤖 Поля `resetTokenHash`/`resetTokenExpiresAt` в `User`; `POST /api/auth/forgot` (всегда 200, rate-limit 3/час по IP и email) + `POST /api/auth/reset` (одноразовый токен, bcrypt 12); страницы `/forgot` и `/reset/[token]`; ссылка «Забыли пароль?» на `/login` | ✅ |

> **Остаётся доработать в 2.2:** инвалидация активных JWT-сессий после сброса
> (требует `passwordChangedAt` + проверка в jwt-callback — вынесено как follow-up,
> т.к. затрагивает горячий путь авторизации). Сам сброс пароля работает.

---

## Фаза 1 — Блокеры, инфраструктура ✅ КОД/СКРИПТЫ ГОТОВЫ (остаётся прод-настройка)

| # | Замечание | Разработчик (готово) | Оператор (на сервере) |
|---|---|---|---|
| 1.2 | Бэкапов нет | ✅ `scripts/backup.sh` (pg_dump→S3), `scripts/restore.sh` (неразрушающий проверочный restore), `scripts/monitor.sh` (алерт в Telegram при просрочке), раздел DEPLOY §8.5, переменные в `.env.example` | Бакет `polka-backups` + отдельный сервисный аккаунт; **1 проверенный restore до запуска**; versioning на `polka-files`; cron `0 3 * * *` |
| 1.3 | Cron эскроу/AI-ревью не настроен | ✅ Модель `CronHeartbeat` + запись в обоих cron-роутах; crontab-строки + §8.5 DEPLOY; `monitor.sh` алертит при просрочке heartbeat | `crontab -e` (эскроу — раз в час, AI-ревью — раз в 5 мин), проверка логов |

## Фаза 1 — Важное, инфраструктура

| # | Замечание | Разработчик | Оператор |
|---|---|---|---|
| 2.3 | Пароль БД / firewall | (override из Фазы 0) | `openssl rand -base64 24` пароль в проде, `ufw allow 22,80,443 && ufw enable` |
| 2.4 | Внешний uptime | — | UptimeRobot/Betterstack на `/api/health` → алерт в Telegram |
| 2.5 | Заголовки в nginx | 🔧 готовый server-блок 443 | вставить в конфиг nginx |

---

## Фаза 2 — Юридический контур ✅ КОД ГОТОВ (остаётся контент)

| # | Замечание | Разработчик (готово) | Контент (на вас) |
|---|---|---|---|
| 2.1 | Нет оферты/политики/соглашения | ✅ Страницы `/legal/offer`, `/legal/privacy`, `/legal/terms` (Server Components, структура «площадка-агент», баннер «черновик») + общий layout; ссылки в футере; чекбокс согласия при регистрации (кнопка задизейблена без него) + **бэкенд-проверка** `agreedToTerms === true` в `POST /api/auth/register` | 📄 **Реальные тексты от юриста** (заменить placeholder `[…]`) + реквизиты ИП/ООО в футере и на страницах |

---

## Фаза 3 — После запуска (не блокеры)

| # | Замечание | Объём |
|---|---|---|
| 3.1 | Redis rate-limit при горизонтальном масштабировании (`@upstash/ratelimit`) | отложить до 2+ инстансов |
| 3.2 | Email-верификация при регистрации (`emailVerified` + письмо с токеном; ограничить отзывы/вопросы неверифицированным) | 🤖 M |
| 3.3 | Формализация выплат: статусы `REQUESTED → PROCESSING → PAID/REJECTED`, SLA в FAQ `/sell`, двойная проверка суммы на момент исполнения | 🤖 M |
| — | CSP через `Content-Security-Policy-Report-Only` (аккуратно из-за inline-скриптов Next.js и Telegram Login Widget) | отдельная задача |

---

## Рекомендованный порядок

1. **Код без внешних зависимостей (разработчик):** 1.1 → 1.4 → 2.4 → 2.5 → 2.3 (override) → 3.1 → 2.2.
   Закрывает 2 из 4 блокеров и 3 важных пункта чистым кодом.
2. **Параллельно (оператор/владелец):** завести бакет бэкапов (1.2) и запустить работу по юр-текстам (2.1) — самое долгое по срокам.
3. **На деплое (оператор):** crontab (1.3), ufw + пароль БД (2.3), uptime-монитор (2.4), restore-тест (1.2), заголовки в nginx (2.5).
4. **После запуска:** 3.2, 3.3, CSP, Redis-лимитер.

---

## Уже сделано ранее (не дублировать)

- Открытый редирект в `/login` — **закрыт**.
- Валидация `CRON_SECRET` (≥32 символов, 503 если не задан) — **добавлена** (пункт 1.3 — про *планирование* cron на сервере, остаётся).
- Метаданные вебхука берутся из `verified` re-fetch ЮKassa — **исправлено**.

---

## Чеклист блокеров перед запуском

- [x] 1.1 Вебхук ЮKassa: CIDR + `X-Real-IP` — **код готов** (nginx уже прокидывает `X-Real-IP`)
- [~] 1.2 Ночной pg_dump в отдельный бакет + проверенный restore + versioning `polka-files` — **скрипты готовы** (`backup.sh`/`restore.sh`/`monitor.sh`); на проде: бакет, ключи, cron, restore-тест
- [~] 1.3 Crontab: эскроу (раз в час) + AI-ревью (раз в 5 мин) — **heartbeat + строки готовы**; на проде: `crontab -e`
- [x] 1.4 Прод-сид без тест-учёток; реальный админ отдельным скриптом; проверка отсутствия `*@polka.test` — **код готов**
- [~] 2.1 Оферта/политика/соглашение + чекбокс согласия + реквизиты — **код готов** (страницы, чекбокс, бэкенд-проверка); остаётся вставить тексты юриста и реквизиты
- [x] 2.2 Восстановление пароля — **готово** (кроме инвалидации старых сессий)
- [~] 2.3 Postgres без проброса порта, стойкий пароль, ufw — **код/override готов**; на проде задать пароль + `ufw`
- [~] 2.4 `/api/health` + healthcheck — **готово**; остаётся подключить внешний uptime-мониторинг
- [x] 2.5 Security-заголовки — **готово** (next.config + nginx-блок в DEPLOY)

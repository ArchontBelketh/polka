# План защиты персональных данных (ПДн)

Цель — минимизировать риск утечки ПДн и последствий (оборотные штрафы 152-ФЗ).
Принцип: даже при полном дампе БД злоумышленник не должен получить ПДн в
пригодном виде.

---

## Что мы храним (ПДн)

- **User**: email, телефон, ИНН, telegram, `passwordHash` (bcrypt ✅)
- **PayoutProfile**: ФИО (`displayName`), ИНН, телефон
- **Переписка/тикеты**: свободный текст — может содержать ПДн
- **Карточные данные НЕ храним** — их держат Т-Банк / CloudKassir ✅
  (снимает самый опасный класс утечек)

## Что уже сделано хорошо

- БД **не проброшена наружу** — доступна только внутри docker-сети
  (`docker-compose.prod.yml` убирает публикацию порта).
- Пароли — **bcrypt**; запросы через **Prisma** (нет SQL-инъекций); **HTTPS**.
- Бэкапы — в **отдельный** S3-бакет с урезанным сервис-аккаунтом
  (`scripts/backup.sh`): утечка ключей приложения не даёт стереть бэкапы.

---

## Оценка исходных идей

| Идея | Вердикт |
|---|---|
| **1. Отдельная БД под ПДн** | На одном VPS выигрыш небольшой: приложение всё равно имеет доступ, взлом приложения = доступ к обеим БД. Сложность высокая, ROI низкий. **Замена: шифрование полей ПДн** даёт тот же эффект дешевле. Отложить. |
| **2. Шифровать БД** | Да, но: «шифрование диска» (at-rest) спасает только от кражи диска/бэкапа, не от взлома приложения. **Ценнее — field-level шифрование отдельных полей** на уровне приложения (ключ не в БД). Дамп БД → бесполезный шифротекст. **Мера №1.** |
| **3. 30-значный пароль БД** | Делаем, быстро. Но т.к. БД не торчит наружу — не главная защита. |
| **4. Доступ по IP** | Уже почти так. Добавить firewall (ufw: 22/80/443), SSH только по ключу. |

---

## План по приоритету

### P0 — быстрые победы (день)

1. **Шифровать бэкапы.** Сейчас `pg_dump | gzip` кладёт ПДн в S3 **в открытом
   виде**. Добавить `gpg`/`age`-шифрование дампа; ключ хранить отдельно от
   бакета. *Утёкший бэкап = вся база — критично.*
2. **Firewall + SSH.** `ufw` (только 22/80/443), SSH только по ключу,
   `fail2ban`, отключить парольный вход.
3. **30-значный пароль БД** + план ротации.
4. **Логи/Sentry без ПДн.** Убедиться, что в Sentry и docker-логи не попадают
   email/телефон/ИНН (сейчас в логах чисто — держать так).

### P1 — ядро (главное технически)

5. **Field-level шифрование ПДн.** AES-256-GCM на уровне приложения для
   `PayoutProfile.inn/phone/displayName` и `User.phone/inn`. Шифруем при записи,
   расшифровываем при чтении; ключ `ENCRYPTION_KEY` (32 байта) — в env, не в БД.
   Утечка дампа → шифротекст.
   - Нюанс: **email не шифруем** (это логин, по нему ищем). Защищаем остальными
     слоями. При необходимости — «слепой индекс» (HMAC) отдельной задачей.
6. **Диск/том at-rest.** Шифрование `pgdata` (LUKS или шифрованный том хостера)
   — защита от кражи диска.
7. **Least privilege для БД.** App-роль не superuser.

### P2 — процессы

8. **Право на удаление (152-ФЗ).** Процедура полного удаления ПДн пользователя
   по запросу.
9. **Ретеншн.** Удалять старые тикеты/сообщения/неподтверждённые аккаунты по
   расписанию (меньше данных — меньше риск).
10. **План реагирования на инцидент** + проверенное восстановление из бэкапа
    (тест-restore).

### P3 — юридический контур (именно он рождает штрафы)

11. **Локализация данных (ст. 18 152-ФЗ).** Сервер должен быть в **РФ** —
    проверить физическое расположение VPS.
12. **Уведомление в Роскомнадзор** об обработке ПДн (оператор ПДн).
13. **Соглашения с обработчиками** (Т-Банк, CloudKassir, хостинг,
    email/Telegram) — они обрабатывают ПДн по нашему поручению.
14. **Политика ПДн** (`/legal/privacy`) + согласие (`agreedToTerms`) —
    актуализировать под реальный состав данных.

---

## Рекомендация: с чего начать

Две меры превращают «утёкшая база = катастрофа» в «утёк бесполезный шифротекст»:

- **P0.1** — шифрование бэкапов (`backup.sh`).
- **P1.5** — field-level шифрование ИНН / телефона / ФИО.

Остальное — обвязка вокруг них.

### Заметки по реализации P1.5

- Хелпер `src/lib/crypto-pii.ts`: `encrypt(text)` / `decrypt(cipher)` на
  AES-256-GCM, формат хранения `v1:<iv>:<tag>:<ciphertext>` (base64), версия
  ключа в префиксе — для будущей ротации.
- Ключ `ENCRYPTION_KEY` (32 байта, base64) в env; **не коммитить**, не класть
  в БД. Потеря ключа = потеря доступа к зашифрованным полям (бэкап ключа
  обязателен, но отдельно от бэкапа БД).
- Точки записи/чтения: `requisites` (PayoutProfile), формирование чеков
  (`receipts.ts` читает `inn/phone/displayName`) — расшифровывать там.
- Миграция существующих строк: единоразовый скрипт «зашифровать то, что лежит
  в открытом виде».
